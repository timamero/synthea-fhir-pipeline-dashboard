import '@mantine/core/styles.css';
import '@mantine/charts/styles.css';

import { MantineProvider } from '@mantine/core';
import { Title, Container, Stack } from '@mantine/core';

import ConditionsCountChartCard from './components/ConditionsCountChartCard';
import CustomText from './components/CustomText';
import { APP_CONFIG } from './config';
import classes from './App.module.css';

export default function App() {
  return (
    <MantineProvider>
      <Container my="xl">
        <Stack justify="center" align="center" mb="xl">
          <Title className={classes.title}>{APP_CONFIG.TITLE}</Title>
          <CustomText ta="center">
            A small dashboard exploring synthetic patient data generated by
            Synthea
          </CustomText>
        </Stack>

        <ConditionsCountChartCard />
      </Container>
    </MantineProvider>
  );
}
